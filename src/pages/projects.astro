---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageHero from '../components/PageHero.astro';
import CTASection from '../components/CTASection.astro';
import { projectCategories, projectPlaceholders } from '../data/site';
---

<BaseLayout
  title="Flooring Project Gallery | Stone Coating & Resinous Oklahoma"
  description="Explore residential, commercial, and industrial flooring renovation projects completed by Stone Coating & Resinous across Oklahoma."
>
  <PageHero
    eyebrow="Project Gallery"
    title="Flooring Projects Across Oklahoma"
    subtitle="Explore residential, commercial, and industrial flooring renovation projects completed by Stone Coating & Resinous."
  />

  <section class="py-16 md:py-24 bg-white">
    <div class="max-w-7xl mx-auto px-6 lg:px-10">
      <!-- Filters -->
      <div class="rounded-lg border border-silver bg-ice-gray p-2 shadow-gloss-card">
        <ul id="project-filters" class="flex flex-wrap gap-1" role="tablist">
          {
            projectCategories.map((cat, idx) => (
              <li>
                <button
                  type="button"
                  class:list={[
                    'project-filter px-4 py-2 rounded-full text-xs uppercase tracking-[0.18em] font-semibold transition-all duration-300',
                    idx === 0
                      ? 'bg-button-gradient text-white shadow-gloss-button'
                      : 'text-navy/70 hover:text-royal-blue hover:bg-white',
                  ]}
                  data-filter={cat}
                  aria-pressed={idx === 0 ? 'true' : 'false'}
                >
                  {cat}
                </button>
              </li>
            ))
          }
        </ul>
      </div>

      <!-- Grid -->
      <div id="project-grid" class="mt-10 grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {
          projectPlaceholders.map((p) => (
            <article
              class="project-card group relative overflow-hidden rounded-lg border border-silver bg-white shadow-gloss-card hover:-translate-y-1 hover:shadow-gloss-glow hover:border-sky-blue/50 transition-all duration-300 flex flex-col cursor-pointer"
              data-category={p.category}
              data-type={p.type}
              data-title={p.title}
              data-service={p.service}
              data-location={p.location}
              data-image={p.image ?? ''}
            >
              <span class="absolute top-0 left-0 right-0 h-[3px] bg-gloss-accent z-10"></span>
              <div class="relative aspect-[4/3] overflow-hidden bg-deep-blue">
                {p.image ? (
                  <img
                    src={p.image}
                    alt={p.title}
                    class="absolute inset-0 w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
                    loading="lazy"
                    decoding="async"
                  />
                ) : (
                  <div class="absolute inset-0 bg-dark-card flex items-end p-5">
                    <span class="text-sky-blue/60 font-heading text-[10px] tracking-[0.28em] uppercase">Photo Coming Soon</span>
                  </div>
                )}
                <div class="absolute inset-0 bg-gradient-to-t from-navy/85 via-navy/10 to-transparent group-hover:from-royal-blue/55 group-hover:via-royal-blue/10 transition-all duration-500"></div>
                <div class="absolute inset-0 bg-card-shine opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
              </div>
              <div class="p-6 flex-1 flex flex-col">
                <p class="text-royal-blue text-[10px] font-semibold uppercase tracking-[0.28em]">{p.type}</p>
                <h3 class="mt-3 font-heading font-semibold text-navy text-lg leading-snug">{p.title}</h3>
                <p class="mt-2 text-ink/70 text-sm flex-1">
                  {p.service} &middot; {p.location}
                </p>
                <span class="mt-4 inline-flex items-center text-royal-blue text-[11px] font-semibold uppercase tracking-[0.24em]">
                  View Details <span class="ml-2 transition-transform group-hover:translate-x-1" aria-hidden="true">&rarr;</span>
                </span>
              </div>
            </article>
          ))
        }
      </div>

      <p
        id="project-empty"
        class="hidden mt-12 text-center text-ink/60 italic"
      >
        No projects in this category yet. Real project photos and videos will be added soon.
      </p>

      <p class="mt-16 text-ink/60 text-sm italic text-center max-w-2xl mx-auto">
        Real project photos and videos coming soon. Below is a preview of the type of work we deliver.
      </p>
    </div>
  </section>

  <!-- Lightbox modal -->
  <div
    id="project-modal"
    class="fixed inset-0 z-[60] hidden items-center justify-center bg-navy/90 backdrop-blur-sm p-6"
    role="dialog"
    aria-modal="true"
    aria-labelledby="project-modal-title"
  >
    <button
      id="project-modal-close"
      type="button"
      class="absolute top-6 right-6 w-10 h-10 inline-flex items-center justify-center text-white hover:text-sky-blue"
      aria-label="Close"
    >
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
        <path d="M6 6l12 12M18 6l-12 12" stroke-linecap="square" />
      </svg>
    </button>
    <div class="relative overflow-hidden rounded-lg bg-white border border-silver max-w-2xl w-full p-8 md:p-10 shadow-gloss-glow">
      <span class="absolute top-0 left-0 right-0 h-[3px] bg-gloss-accent"></span>
      <p id="project-modal-eyebrow" class="text-royal-blue text-[11px] font-semibold uppercase tracking-[0.22em]"></p>
      <h2 id="project-modal-title" class="mt-2 font-heading font-semibold text-navy text-2xl md:text-3xl leading-tight tracking-tight"></h2>
      <p id="project-modal-meta" class="mt-3 text-ink/70 text-sm"></p>
      <div id="project-modal-image" class="mt-8 relative aspect-video overflow-hidden rounded-md bg-deep-blue">
        <div class="modal-empty absolute inset-0 bg-dark-card flex items-end p-5">
          <span class="text-sky-blue/60 font-heading text-[10px] tracking-[0.28em] uppercase">Photo Coming Soon</span>
        </div>
        <img class="modal-photo hidden absolute inset-0 w-full h-full object-cover" alt="" />
        <div class="absolute inset-0 bg-card-shine pointer-events-none"></div>
      </div>
      <p class="mt-6 text-ink/75 text-sm leading-relaxed">
        Detailed project photos, videos, and before-and-after views will be added soon. To inquire about a similar project for your space, request a quote.
      </p>
      <div class="mt-8">
        <a
          href="/contact"
          class="inline-flex items-center px-6 py-3 rounded-full bg-button-gradient text-white text-xs font-semibold uppercase tracking-[0.2em] border border-white/25 shadow-gloss-button hover:shadow-[0_18px_40px_rgba(20,92,255,0.42)] hover:-translate-y-0.5 transition-all duration-300"
        >
          Request a Similar Project
        </a>
      </div>
    </div>
  </div>

  <CTASection />
</BaseLayout>

<script>
  const filters = document.querySelectorAll<HTMLButtonElement>('.project-filter');
  const cards = document.querySelectorAll<HTMLElement>('.project-card');
  const empty = document.getElementById('project-empty');

  const ACTIVE = ['bg-button-gradient', 'text-white', 'shadow-gloss-button'];
  const INACTIVE = ['text-navy/70', 'hover:text-royal-blue', 'hover:bg-white'];

  const applyFilter = (value: string) => {
    let visible = 0;
    cards.forEach((card) => {
      const match = value === 'All' || card.dataset.category === value;
      card.classList.toggle('hidden', !match);
      if (match) visible++;
    });
    if (empty) empty.classList.toggle('hidden', visible !== 0);
  };

  filters.forEach((btn) => {
    btn.addEventListener('click', () => {
      filters.forEach((other) => {
        other.classList.remove(...ACTIVE);
        other.classList.add(...INACTIVE);
        other.setAttribute('aria-pressed', 'false');
      });
      btn.classList.add(...ACTIVE);
      btn.classList.remove(...INACTIVE);
      btn.setAttribute('aria-pressed', 'true');
      applyFilter(btn.dataset.filter ?? 'All');
    });
  });

  // Lightbox
  const modal = document.getElementById('project-modal');
  const modalClose = document.getElementById('project-modal-close');
  const modalEyebrow = document.getElementById('project-modal-eyebrow');
  const modalTitle = document.getElementById('project-modal-title');
  const modalMeta = document.getElementById('project-modal-meta');

  const openModal = (card: HTMLElement) => {
    if (!modal || !modalTitle || !modalMeta || !modalEyebrow) return;
    modalEyebrow.textContent = card.dataset.type ?? '';
    modalTitle.textContent = card.dataset.title ?? '';
    modalMeta.textContent = `${card.dataset.service} · ${card.dataset.location}`;
    const photo = modal.querySelector<HTMLImageElement>('.modal-photo');
    const emptyEl = modal.querySelector<HTMLElement>('.modal-empty');
    const src = card.dataset.image;
    if (photo && emptyEl) {
      if (src) {
        photo.src = src;
        photo.alt = card.dataset.title ?? '';
        photo.classList.remove('hidden');
        emptyEl.classList.add('hidden');
      } else {
        photo.removeAttribute('src');
        photo.classList.add('hidden');
        emptyEl.classList.remove('hidden');
      }
    }
    modal.classList.remove('hidden');
    modal.classList.add('flex');
    document.body.style.overflow = 'hidden';
  };

  const closeModal = () => {
    if (!modal) return;
    modal.classList.add('hidden');
    modal.classList.remove('flex');
    document.body.style.overflow = '';
  };

  cards.forEach((card) => {
    card.addEventListener('click', () => openModal(card));
  });

  modalClose?.addEventListener('click', closeModal);
  modal?.addEventListener('click', (e) => {
    if (e.target === modal) closeModal();
  });
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeModal();
  });
</script>
